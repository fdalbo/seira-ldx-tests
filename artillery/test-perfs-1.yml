config:
  target: http://localhost:2020/
  # Load the Playwright engine:
  engines:
    playwright: {}
  # Path to JavaScript file that defines Playwright test functions
  processor: "../playwright/test-perfs-1.js"
scenarios:
  - engine: playwright
    testFunction: "test1"